import AppContainer from "./AppContainer";
import css from "./page.module.scss";

const Home = () => {
  return (
    <main className={css.main}>
      <AppContainer />
    </main>
  );
};

export default Home;
